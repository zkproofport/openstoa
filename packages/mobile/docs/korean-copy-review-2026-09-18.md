# 모바일 한국어 문구 검토 — 2026-09-18

검토 범위: `src/i18n/locales/ko.json`의 기존 620개 문구 전체와 대응 영어 원문, 사용 화면 및 오류 표시 경로. 기존 작업 내용은 유지했다.

- 기존 문구 620개 검토, 312개 수정.
- 현지화 누락 47개 추가. 한국어·영어 각각 총 667개 키.
- 피드, 토픽, 게시물·댓글, 프로필, 배지, 1:1 대화, 채팅, 초대, AI 권한, 알림, 복구, 기기 데이터 삭제, 온보딩 및 로그인 화면을 검토했다.
- 게시물 / 댓글 / 토픽 / 인증 배지 / 신원 인증 / 로그인 인증 / 워크스페이스 인증 / 복구 코드 / 초대제 / 비밀 / 1:1 대화로 통일했다. 버튼은 짧은 동작 표현, 안내는 합니다·해 주세요 형태로 정리했다.
- 배지 공개·숨김은 인증 취소가 아님을 명시했다. 프로필 통계의 분리된 숫자 뒤에 붙던 ‘개 글’, ‘회 기록’을 독립 라벨로 바꿨다.
- API 권한과 채팅 기록 범위, 기본 카테고리, 알림 버튼 접근성, 이미지 닫기, 태그 입력, 투표 미리보기, 복구 완료 문구의 하드코딩을 번역 키로 연결했다. 사용자 작성 내용·사용자 지정 카테고리·명령 경로·도메인·브랜드는 번역하지 않는다.
- 오류는 표시 경계에서 현지화한다. 원본 오류/HTTP 상태/진단 로그는 유지한다. 연결 실패, 시간 초과, 권한, 잘못된 입력, 요청 제한을 구분하며 알 수 없는 오류에 임의의 네트워크 원인을 붙이지 않는다. 닉네임 중복 및 예약어, 복구 코드, 초대 코드 오류는 해당 조치를 안내한다.
- 복구 안내의 ‘키가 이 휴대폰에만 있다’, ‘누구도 복구할 수 없다’ 같은 무조건적 표현은 해당 기기의 백업 여부와 키 유실 조건을 중심으로 수정했다. 공개 토픽은 서비스가 기록 키를 보유하며 초대제·비밀·1:1 대화는 다른 기기의 키 공유가 필요할 수 있음을 유지했다.

## 검증

Node 22.23.2 사용. 2026-09-18 13:56 KST 기준:

- 모바일 전체 Vitest: **122개 파일, 1,545개 테스트 통과**.
- 모바일 `tsc --noEmit -p packages/mobile/tsconfig.json`: 통과.
- EN/KO 키 667개 및 모든 보간 변수 일치.
- `git diff --check -- packages/mobile`: 통과.

첫 실행의 문구 assertion 실패 9건은 모두 이번 문구/현지화 변경에 따른 기대값 불일치(A)였다. `badgeVisibilitySettings.test.tsx:69`, `identityBadges.test.tsx:20,70`의 인증/공개 명칭, `aRefusedReadDoesNotUndoARecovery.test.ts:77`, `recoveryPromisesNothingFalse.test.ts:140`의 복구 안내 표현, `queryErrorState.test.tsx:52,79,93`의 서버 원문 표시 기대, `signInHang.test.tsx:312`의 원문 오류 기대를 새 의미에 맞게 갱신했다. 서버 원문 대신 활성 언어의 안내가 표시되는지 한국어 렌더링 및 양 언어 오류 분류 검증으로 확인했다.

## 경계 및 남은 범위

- 보간값 0/1/큰 값, 닉네임·도메인·코드 등 데이터 값: 보간 변수를 모두 보존했다. 개인정보/자유 작성 문자열의 번역은 하지 않았다.
- 빈 값·객체·잘못된 서버 응답·권한·시간 초과·요청 제한: 기존 테스트 및 오류 분류 검증으로 확인했다. 테스트에서 오류 재시도 버튼도 실제로 누른다.
- 실제 iOS/Android 기기에서 줄바꿈, 폰트 크기, 화면 읽기 기능을 확인하지 않았다. 이 검토는 소스 및 렌더 테스트 기준이다.
- 로그인 호스트 앱 자체의 화면/에러 카탈로그, 서버가 작성한 기존 채팅 메시지, 외부 웹 문서는 이 패키지 범위 밖이다. API에 새 권한이나 사용자 지정 카테고리가 추가되면 그 식별자/이름은 그대로 표시한다.
- 로컬 Docker/서버 실행, 데이터 초기화, 커밋은 수행하지 않았다.
